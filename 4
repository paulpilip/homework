#include <iostream>
#include <math.h>

int absolute_value(float x) {
    float a;
    a = fabsf(x);
    return a;
}

int main() {
    float x;
    std::cin >> x;
    std::cout << absolute_value(x) << std::endl;
}
